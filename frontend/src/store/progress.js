const LOAD_PROGRESS = 'progress/LOAD_PROGRESS'

const loadProgresses = (progresses) => ({
  type: LOAD_PROGRESS,
  progresses
})


export const fetchProgresses = () => async dispatch => {
  const response = await 
}
